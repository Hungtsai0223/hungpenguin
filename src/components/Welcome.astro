<main class="home">
  <!-- 背景影片：影片檔請放在 public/ocean-bg.mp4 -->
  <div class="video-bg" aria-hidden="true">
    <video autoplay muted loop playsinline preload="auto">
      <source src="/ocean-bg.mp4" type="video/mp4" />
    </video>
  </div>

  <!-- 霧面遮罩：避免影片太搶眼、讓文字更清楚 -->
  <div class="video-overlay" aria-hidden="true"></div>

  <section class="hero">
    <img class="avatar" src="/profile.jpg" alt="Portrait of Ralf Tsai" />

    <div class="hero-content">
      <p class="kicker">Hey!</p>

      <h1>I'm Ralf</h1>

      <p class="intro">
        Oceanography background exploring environmental change, sustainability communication,
        and public engagement through research and <strong>如果無塑</strong>.
      </p>
    </div>
  </section>

  <section class="brief">
    <p>
      I write about ocean science, sustainability, field notes, and everyday reflections —
      connecting research with the way we live, move, and care for the world.
    </p>
  </section>
</main>

<style>
  .home {
    /* 首頁整體區塊 */
    position: relative;
    display: grid;
    gap: 72px;
    min-height: 72vh;
    z-index: 1;
  }

  /* 讓 header / footer 不會被背景影片蓋住 */
  :global(.site-header),
  :global(.site-footer) {
    position: relative;
    z-index: 5;
  }

  /* 背景影片 */
  .video-bg {
    position: fixed;
    inset: 0;

    /* 不要用負值，否則會被 body 背景蓋掉 */
    z-index: 0;

    overflow: hidden;
    pointer-events: none;
  }

  .video-bg video {
    width: 100%;
    height: 100%;
    object-fit: cover;

    /* 影片可見度：越高越明顯；建議 0.28–0.45 */
    opacity: 0.9;

    /* 影片質感調整：
       brightness 亮度
       contrast 對比
       saturate 飽和度
       blur 模糊程度 */
    filter: brightness(0.82) contrast(0.9) saturate(0.7) blur(0.2px);
  }

  /* 影片上方遮罩 */
  .video-overlay {
    position: fixed;
    inset: 0;
    z-index: 1;
    pointer-events: none;

    /* 這裡控制整體霧面感：
       rgba 最後一個數字越高，影片越淡
       想讓影片更明顯：0.68 改 0.55
       想讓文字更清楚：0.68 改 0.75 */
    background:
      linear-gradient(
        90deg,
        rgba(251, 252, 248, 0.82) 0%,
        rgba(251, 252, 248, 0.70) 48%,
        rgba(251, 252, 248, 0.62) 100%
      ),
      radial-gradient(
        circle at 22% 20%,
        rgba(168, 200, 240, 0.26),
        transparent 36%
      );
  }

  .hero,
  .brief {
    position: relative;
    z-index: 2;
  }

  .hero {
    display: grid;

    /* 左欄寬度：要跟頭貼大小接近 */
    grid-template-columns: 210px 1fr;

    /* 頭貼與文字距離 */
    gap: 46px;

    align-items: center;
    margin-top: 24px;
  }

  .avatar {
    /* 首頁頭貼尺寸：想更大就調這兩個 */
    width: 210px;
    height: 210px;

    object-fit: cover;
    object-position: center;
    border-radius: 999px;

    border: 5px solid rgba(255, 255, 255, 0.9);
    box-shadow: 0 24px 58px rgba(26, 39, 68, 0.14);
  }

  .kicker {
    margin: 0 0 10px;
    color: #6c88ad;

    /* Hey! 字體大小在這裡調整 */
    font-size: 24px;

    font-weight: 700;
  }

  h1 {
    margin: 0;
    color: #1a2744;

    /* I'm Ralf 字體大小在這裡調整 */
    font-size: clamp(56px, 8vw, 88px);

    line-height: 1;
    letter-spacing: -0.055em;
    font-weight: 800;
  }

  .intro {
    max-width: 640px;
    margin: 22px 0 0;
    color: rgba(26, 39, 68, 0.72);

    /* 首頁介紹文字大小 */
    font-size: 21px;

    line-height: 1.65;
  }

  .intro strong {
    color: #1a2744;
    font-weight: 800;
  }

  .brief {
    max-width: 720px;
    padding-top: 20px;
    border-top: 1px solid rgba(26, 39, 68, 0.12);
  }

  .brief p {
    margin: 0;
    color: rgba(26, 39, 68, 0.66);

    /* brief 文字大小 */
    font-size: 18px;

    line-height: 1.8;
  }

  @media screen and (max-width: 720px) {
    .hero {
      grid-template-columns: 1fr;
      gap: 24px;
    }

    .avatar {
      width: 150px;
      height: 150px;
    }

    h1 {
      font-size: clamp(44px, 14vw, 64px);
    }

    .intro {
      font-size: 18px;
    }

    .video-bg video {
      /* 手機版影片淡一點，避免太亂 */
      opacity: 0.28;
    }

    .video-overlay {
      background: rgba(251, 252, 248, 0.78);
    }
  }
</style>